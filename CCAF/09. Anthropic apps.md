- Claude Code: 터미널에서 실행되는 agent 형 coding assistant
	- 파일 편집 및 버그 수정
	- 코딩 질문에 답하기, 개발 workflow 지원 등
- Computer Use: Claude 가 Desktop app 과 상호작용 할 수 있게 해주는 Tool set
	- 웹 사이트에 접근하고 인터넷을 탐색
	- 시각적 인터페이스 탐색이 필요한 작업 수행
	- 타 앱과 상호작용

이 두 가지는 agent 를 효과적으로 만드는 핵심 원칙들을 보여준다.

- tool 통합 및 사용
- 다단계 작업 실행
- 환경과의 상호작용
- 자율적 문제 해결

## Claude Code 를 통해 할 일

### /init

Claude Code 프로젝트에서 작업을 시작할 땐 가장 먼저 `/init` command 를 입력한다. Claude 가 전체 코드베이스를 스캔하고 프로젝트의 구조, 아키텍처 등을 이해한다. 그 후 `CLAUDE.md` 파일을 생성해서 context 를 관리한다.

(context 를 날리고 싶다면 `/clear`)

`CLAUDE.md` 는 다음과 같은 계층 구조로 나눠 만들어서 활용할 수 있다.

- **Project**: 프로젝트에서 작업하는 모든 엔지니어 간에 공유됨
- **Local**: git에 체크인되지 않은 개인 노트
- **User**: 모든 프로젝트에 걸쳐 사용됨

### context 주입

Claude 에게 무언가를 요청하기 전에 정확한 context (코드베이스 파일 등) 를 주입하자. 그럼 기존 코딩 패턴과 활용할 수 있는 기존 기능 예시들을 확보할 수 있다.

### 솔루션 계획 세우기 지시
바로 구현으로 넘어가기 보다, Plan mode 를 활용해서 구체적인 계획을 세우도록 유도하자.

### 솔루션 구현
구체적이고 견고한 계획이 세워지면 그 후부터 구현을 요청하자.

## MCP Server 를 통한 확장
- Claude Code 는 내부적으로 MCP Client 가 내장되어 있다.
- 이를 활용해 MCP Server 에 연결해서 수행 작업을 대폭 확장할 수 있다.

```bash
# claude mcp add [server-name] [command-to-start-server]
$ claude mcp add documents uv run main.py
```

MCP Server 를 추가하는 방법은 간단하다. 쉘에서 위와 같이 서버를 등록해주면 된다.
다음 Claude session 이 시작될 때 자동으로 MCP Client 를 통해 MCP Server 와 connection 을 준비한다.

